name: merge-check.yml
on:
  pull_request:

jobs:
  tests:
    uses: ./.github/workflows/go-tests.yml
    with:
      go-version-file: 'go.mod'

  build:
    uses: ./.github/workflows/go-build.yml
    with:
      go-version-file: 'go.mod'
      working-directory: 'cmd/8stash'